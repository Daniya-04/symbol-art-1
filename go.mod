module symbol-art

go 1.26.4

// TODO (RU): Разрешены только стандартные пакеты Go (без внешних
// зависимостей) — секция require здесь появляться не должна.
// TODO (EN): Only standard Go packages are allowed (no external
// dependencies) — no require section should appear here.
